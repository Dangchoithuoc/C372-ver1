<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login | The Bookmark</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="auth-body">
  <div class="auth-card">
    <div class="pill">The Bookmark</div>
    <h1>Welcome back</h1>
    <p class="auth-muted">Sign in to view your cart and picks.</p>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="POST" action="/login">
      <div>
        <label for="email">Email</label><br>
        <input id="email" name="email" type="email" placeholder="you@example.com" required>
      </div>
      <div>
        <label for="password">Password</label><br>
        <input id="password" name="password" type="password" placeholder="********" required>
      </div>
      <button class="auth-cta" type="submit">Log in</button>
    </form>

    <p class="auth-muted">New here? <a href="/register">Create an account</a></p>
    <p class="auth-muted"><a href="/">Back to homepage</a></p>
  </div>
</body>
</html>
